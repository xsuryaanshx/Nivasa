# Environment variables for Nivasa
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git. This file (.env.example) is safe to commit.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GEMINI_API_KEY=   # Get from https://aistudio.google.com/app/apikey — used for payment screenshot OCR
